// Re-exports from AuthProvider so import paths remain unchanged across the codebase.
export { useAuth } from '@/providers/AuthProvider'
export type { AuthContextValue } from '@/providers/AuthProvider'
